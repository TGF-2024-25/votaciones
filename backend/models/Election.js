export default class Election {

    constructor(id = null, title, photourl, initialDate, finalDate) {
        this.id = id;
        this.title = title;
        this.photourl = photourl;
        this.initialDate = initialDate;
        this.finalDate = finalDate;
       
    }
}
  